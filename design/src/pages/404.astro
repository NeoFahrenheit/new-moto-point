---
import Base from '../layouts/Base.astro';
import Icone from '../components/Icone.astro';
import { site, contato, zap } from '../data/site';
import { url } from '../data/navegacao';

const atalhos = [
  { href: '/loja/', rotulo: 'Catálogo da loja', icone: 'loja' },
  { href: '/loja/#ofertas', rotulo: 'Ofertas', icone: 'estrela' },
  { href: '/oficina/', rotulo: 'Serviços da oficina', icone: 'ferramentas' },
  { href: '/loja/#pecas', rotulo: 'Peças de reposição', icone: 'motor' },
  { href: '/loja/#chuva', rotulo: 'Acessórios', icone: 'bau' },
  { href: '/contato/', rotulo: 'Endereço e horários', icone: 'local' },
];
---

<Base
  titulo={`Página não encontrada — ${site.nome}`}
  descricao="A página que você procurou não existe mais. Veja as ofertas, os serviços da oficina ou fale com a New Moto Point no WhatsApp."
  semIndexacao
>
  <section class="relative overflow-hidden bg-carbon-950">
    <div class="textura-pontos absolute inset-0 opacity-60"></div>
    <div class="absolute -top-32 -right-24 size-[32rem] rounded-full bg-rubi-600/20 blur-[130px]"></div>

    <div class="relative mx-auto max-w-3xl px-4 py-24 text-center sm:px-6 lg:py-32">
      <p class="display numero-vazado text-[24vw] leading-none sm:text-[11rem]">404</p>

      <h1 class="display -mt-4 text-3xl text-white sm:-mt-8 sm:text-5xl">
        Essa página <span class="text-rubi-500">saiu de linha.</span>
      </h1>

      <p class="mx-auto mt-5 max-w-lg text-[15px] leading-relaxed text-white/60 sm:text-base">
        O endereço que você abriu não existe mais. Mas a loja continua no mesmo lugar, na Av.
        Farrapos — escolha um atalho abaixo ou chama no WhatsApp que a gente resolve.
      </p>

      <div class="mt-9 flex flex-col justify-center gap-3 sm:flex-row">
        <a
          href={url('/')}
          class="group flex items-center justify-center gap-2.5 rounded-sm bg-rubi-500 px-7 py-4 font-extrabold tracking-wide text-white uppercase transition-all hover:bg-rubi-600"
        >
          <Icone nome="moto" class="size-5.5" />
          Voltar para o início
        </a>
        <a
          href={zap('Olá! Caí numa página que não existe no site. Preciso de ajuda com: ')}
          target="_blank"
          rel="noopener"
          class="flex items-center justify-center gap-2.5 rounded-sm bg-zap-500 px-7 py-4 font-extrabold tracking-wide text-carbon-950 uppercase transition-all hover:bg-zap-400"
        >
          <Icone nome="whatsapp" class="size-5.5" />
          Falar no WhatsApp
        </a>
      </div>

      <ul class="mt-12 flex flex-wrap justify-center gap-2.5">
        {
          atalhos.map((a) => (
            <li>
              <a
                href={url(a.href)}
                class="flex items-center gap-2 rounded-sm border border-white/12 bg-white/4 px-4 py-2.5 text-sm font-semibold text-white/75 transition-all hover:border-rubi-500 hover:bg-rubi-500/12 hover:text-white"
              >
                <Icone nome={a.icone} class="size-4.5 text-rubi-400" />
                {a.rotulo}
              </a>
            </li>
          ))
        }
      </ul>

      <p class="mt-10 text-sm text-white/40">
        Ou ligue para <a
          href={contato.telefoneFixoHref}
          class="font-semibold text-white underline decoration-rubi-500 decoration-2 underline-offset-4 transition-colors hover:text-rubi-400"
          >{contato.telefoneFixo}</a
        >.
      </p>
    </div>
  </section>
</Base>
